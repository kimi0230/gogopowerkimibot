name: Wake Up Heroku
on:
  schedule:
    # - cron: "30 23 * * *"
    - cron: "*/5 * * * *"
jobs:
  WakeUp-Actions:
    runs-on: ubuntu-latest
    env:
      HEROKU_URL: ${{ secrets.HEROKU_URL }}
    steps:
      - name: call heroku
        run: curl  "%HEROKU_URL%"
